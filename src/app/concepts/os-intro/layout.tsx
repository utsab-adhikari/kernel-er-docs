import React from "react";

export const metadata = {
	title: "OS Intro — Foundations · kernel-er Concepts",
	description:
		"Foundational overview of what an operating system is, why it exists, and how it virtualizes hardware.",
};

export default function OsIntroLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return children;
}

